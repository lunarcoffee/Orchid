var i: Number;
var j: Array<Number> = getNumArr;
var s: String = "String";

print("Orchid");
print(s);
s = "String again";
print(s);

var m: Number = getNumber(-1239.2) * 2 ** 5 ** 3 + 2;
var k: Number = 3 + m * 2 / (1 - getNumber(3)) ** -2 ** 3;

when (s + "!") {
    "Number" -> print("number");
    "String" -> {
        var a: Number = 1;
        print(a);
    }
    "hello", "String!" -> print("string!");
    else -> print("error!");
}

# This is a comment!
func print(msg: Any): Void {
    js.console.log(msg);
}

func getNumArr(): Array<Number> {
    var number: Number = 1;
    var arr: Array<Number> = []Number{number, 2 & 1, 4 + +"5"};

    {
        arr = []Number{1, number, 3};
        var a: Number = 1;
        arr = []Number{a, number, +"339" >| 3};
    }

    when (number + 1) {
        in arr -> print("in arr!");
        in []Number{1, 2, 3} -> print("in array literal!"); # Eventually replace with range syntax.
    }
    return arr;
}

var b: Boolean = false;
if (b || false)
    var a: Number = 2; # Test redefinition checks.
else if (!(s != "String") && j == []Number{1, 1, 3})
    print("hi");
else {
    var a: Number = 1;
    print(a);
}

func getNumber(arg: Number): Number {
    return arg + 53942.233 ** 0.1;
}
